services:
  - type: web
    name: receipt-ocr-annotator
    env: python
    plan: free
    buildCommand: uv sync --frozen && uv run python -m compileall -q .
    startCommand: uv run uvicorn app:app --host 0.0.0.0 --port $PORT

